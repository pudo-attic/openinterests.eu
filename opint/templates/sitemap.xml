<?xml version="1.0" encoding="UTF-8"?>

<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for i, d, s in entities %}
    <url>
      <loc>{{url_for('entities.view', id=i, _external=True)}}</loc>
      <lastmod>{{d}}</lastmod>
      <changefreq>monthly</changefreq>
      <priority>{{s}}</priority>
    </url>
    {% endfor %}
</urlset> 
